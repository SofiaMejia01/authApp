//el enum le da un valor desde el 0 a cada variable declarada por orden
export enum AuthStatus {
    checking = 'checking',
    authenticated = 'authenticated',
    notAuthenticated = 'notAuthenticated',
    
}